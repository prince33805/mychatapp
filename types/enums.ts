export enum AdminRole {
  ADMIN = 'ADMIN',
  OPERATOR = 'OPERATOR',
}

export enum ConversationStatus {
  OPEN = 'OPEN',
  CLOSED = 'CLOSED',
}

export enum SenderType {
  ADMIN = 'ADMIN',
  CUSTOMER = 'CUSTOMER',
  BOT = 'BOT',
}

export enum MessageType {
  TEXT = 'TEXT',
  IMAGE = 'IMAGE',
  STICKER = 'STICKER',
  FILE = 'FILE',
  RICH = 'RICH',
}
